# Ватэс: задачи и статус

## Как пользоваться этим списком

- `Готово` - работа уже выполнена и была зафиксирована в истории проекта.
- `В работе` - локальные изменения существуют, но еще не завершены проверкой и не залиты.
- `Ожидает` - задача не начата или требует отдельного решения.
- Любая внешняя заливка на GitHub или хост выполняется только по прямой команде пользователя.

## В работе

### Полный перенос визуала Ватэс

Статус: `Готово локально, не опубликовано`.

1. Бренд интерфейса заменен на Ватэс, подключены оригинальные логотипы из предоставленного прототипа.
2. Светлая тема стала основной; переключатель включает адаптированную темную тему.
3. Перенесены палитра, боковая и верхняя навигация, формы, страница входа, рабочее пространство, сотрудники, профиль, обучение, уроки, тесты, результаты, материалы, каталог и AI.
4. В боковую панель добавлены отдельные разделы `Ассортимент` и `Товары` без удаления прежних функций.
5. AI-помощник переименован в `Ватэс AI`, стандартные эмодзи заменены SVG-иконками.
6. Восстановлен полный футер прошлой версии и адаптирован под светлую и темную темы.
7. Локально проверены рабочее пространство, сотрудники, обучение, материалы, товары и AI в светлой теме, а также рабочее пространство в темной теме.
8. `npx tsc --noEmit` и `npm run build` проходят. После авторизованной проверки `git diff --exit-code -- data` остается пустым.
9. Оригинальный HTML-прототип запущен на `http://localhost:3200/`, текущая production-сборка - на `http://localhost:3000/`.
10. GitHub и хост не обновлять, пока пользователь не даст отдельную прямую команду.
11. Обзор `/admin` и экран `/admin#employees` разделены: в разделе сотрудников остается только управление учетными записями; коммуникации, аттестации, календарь, заметки и дедлайны находятся в рабочем пространстве.
12. Боковая панель адаптирована по контрольным точкам HTML-прототипа: `248px` на широком экране, `84px` и только SVG-иконки до `1100px`, скрытая панель и нижняя навигация до `767px`. Плавающая кнопка меню удалена.
13. Исправлен переход из `/admin#employees` в `/admin`: вкладки рабочего пространства и сотрудников используют надежные обычные ссылки вместо `Next Link`. Оба направления проверены физическим кликом в браузере.
14. Календарь и отправка уведомлений выровнены в одной строке; заметки и дедлайны переработаны в читаемую полноширинную панель ниже. Декоративные квадратные иконки в заголовках удалены.
15. Карточки статистики используют фактические данные, «Требует внимания» не выводит нулевые проблемы, а круговой прогресс заполняется анимацией только при ненулевом значении и перезапускается при смене периода.
16. Проверки после последней правки: `npx tsc --noEmit`, `npm run build`, открытие `/admin` с кодом `200`, работа вкладок аттестации и заметок/дедлайнов, отсутствие ошибок браузера.
17. Карточки заметок и дедлайнов показывают полную дату с месяцем и годом, имеют фиксированную увеличенную высоту для двух строк текста и открывают полное содержание по нажатию. Локально проверено открытие и закрытие окна заметки без ошибок браузера.
18. Личная заметка в календаре разделена на заголовок и подробности: действуют лимиты `120` и `2000` символов, а счетчики видны при вводе. Из пустого состояния «Ближайших событий» убрана декоративная иконка; панель заметок и дедлайнов использует общую минимальную высоту и растет при переполнении.
19. После правки прошли `npx tsc --noEmit`, `npm run build` и `git diff --check`. Локально проверены форма заметки, обновление счетчика и пустая вкладка дедлайнов; тестовый ввод закрыт без сохранения. Отслеживаемые JSON-файлы `data/` не изменены.
20. Переключатель темы на странице входа перенесен из границы двух колонок в верхний правый угол рабочей части. На мобильном экране он остается в верхней части брендового блока справа и не пересекается с логотипом.
21. У карточки входа убраны собственная прокрутка и ограничение высоты: длинная форма регистрации теперь прокручивается вместе со всей страницей. Текст ссылки на регистрацию начинается с «Первый вход?».
22. Над основным заголовком брендовой части страницы входа удалена дополнительная подпись «Обучение и знания команды».
23. Обычная форма входа на компьютере и телефоне не создаёт вертикальную прокрутку: общий футер виден в нижней части того же экрана, а весь сюжетный текст и форма помещаются выше него. Регистрация сохраняет обычную прокрутку для длинной формы.
24. Логотип боковой панели адаптирован: на широком экране показывается название Ватэс, а в компактной панели до `1100px` остаётся только знак без обрезанного текста.

### Унифицированный просмотр документов

Статус: `Готово и развернуто`.

1. Добавлен общий определитель форматов и безопасный декодер UTF-8, UTF-16, Windows-1251 и Windows-1252.
2. Текстовые файлы и файлы с неизвестным расширением больше не зависят от браузерного `iframe` и сохраненного MIME-типа.
3. XLS/XLSX/XLSM/XLSB/ODS, PPTX, ODT/ODP и RTF открываются как извлеченный текст через авторизованный `/api/document-preview`.
4. PDF, DOCX, изображения, видео и аудио используют прежний визуальный просмотр; архивы, исполняемые файлы и старые DOC/PPT остаются для скачивания.
5. Проверены неизвестное расширение, UTF-16, Windows-1251, XLSX и существующий CSV. `npx tsc --noEmit`, точечный ESLint новых файлов и `npm run build` проходят.
6. Функциональный коммит `464a0c0 Improve document previews` отправлен в рабочую ветку и `main`, сервер обновлен fast-forward, production-сборка прошла, PM2-процесс работает.
7. На `tea-hub.ru` авторизованный маршрут прочитал UTF-8-файл с неизвестным расширением и вернул правильный текст; без сессии маршрут возвращает `401`.

### Дедлайны на документы, темы и тесты

Статус: `Готово и развернуто`.

1. В календарной форме добавлен выбор типа материала: документ, тема или тест.
2. Сохранена обратная совместимость со старыми дедлайнами через `linkedTestId`.
3. Открытие теста использует прежние проверки порядка и обязательных аттестаций без изменения алгоритма.
4. Документы и темы открываются существующими маршрутами `documentId` и `routeId`.
5. Поиск защищен от нестроковых названий из хранилища, ошибки сохранения больше не закрывают заполненную форму.
6. Все три типа выбраны в локальном браузере без вылета и ошибок консоли; `npx tsc --noEmit` и `npm run build` проходят.
7. Исправлен случай пустого поля задачи: выбранный материал теперь сам формирует текст дедлайна, поэтому задача действительно записывается и отправляется.

Функциональный коммит `31c25cf` отправлен в рабочую ветку и `main`, сервер обновлен fast-forward. Серверная сборка прошла, PM2-процесс работает, публичная форма дедлайна проверена без ошибок браузера.

Регрессия воспроизведена по серверным данным и исправлена в коммите `21f174b`. Контрольная отправка без ручного текста создала задачу, календарную запись и ссылку на документ; тестовые данные после проверки восстановлены. Коммит отправлен в рабочую ветку и `main`, сервер обновлен, production-сборка прошла, PM2-процесс работает, публичная страница отвечает кодом `200`.

### Удаление автоподмены второго администратора

Статус: `Готово локально, не опубликовано`.

- Найдена причина повторного появления системного администратора: `ensureBaseUsers` считал любую запись `u_staff` устаревшим системным аккаунтом и записывал измененную роль обратно в JSON.
- Автоподмена и автозапись при чтении удалены. Исходный `u_admin` остается администратором, `u_staff` остается обычным сотрудником.
- Исходный `tea_hub_users_v1.json` восстановлен без изменения данных, созданный во время проверки `.bak` удален.
- После повторного запуска и открытия `/admin` JSON не изменился, `.bak` не появился, в таблице отображаются Главный Мастер и Ярик.

### Создание тем и тестов через Alice AI

Статус: `Готово и развернуто`.

Локально готово:

1. Компактный выбор `Создать вручную` или `Создать через Alice AI` у тем и тестов.
2. Загрузка до 8 документов, не более 10 МБ на файл и 25 МБ суммарно.
3. Чтение PDF, DOCX, XLS/XLSX/ODS, PPTX, ODT/ODP, RTF и текстовых форматов.
4. Настройки раздела, детализации, сложности и количества вопросов.
5. Администраторский API с проверкой роли, лимитов и структуры ответа Alice AI.
6. Передача результата в существующий редактор без автоматической публикации.
7. Отдельные светлые и темные стили, адаптивная компоновка окна.
8. Успешные `npx tsc --noEmit` и `npm run build`; новые отдельные файлы проходят ESLint.

Проверка релиза:

1. Функциональный commit `8dedc82` отправлен в рабочую ветку и `main` на GitHub.
2. На хосте установлены новые зависимости, production build прошел, PM2-процесс перезапущен и работает.
3. `https://tea-hub.ru/` отвечает кодом `200`.
4. Публичный AI API принял Markdown-документ и вернул тему из двух блоков без предупреждений.
5. Меню и окно генератора проверены визуально на публичной странице обучения.
6. Тестовый AI-черновик не сохранялся в учебные данные.

Отдельными будущими задачами остаются OCR изображений, распознавание аудио и поддержка старых бинарных DOC/PPT.

## Готово в последних коммитах

| Коммит | Содержание |
| --- | --- |
| `21f174b` | Исправлена отправка дедлайна с выбранным материалом и пустым ручным текстом. |
| `31c25cf` | Дедлайн может открывать документ, тему или тест без изменения старого алгоритма тестов. |
| `8dedc82` | Добавлен генератор тем и тестов из документов через Alice AI. |
| `6bdba43` | Исправлен переключатель режима аккаунта в светлой теме. |
| `5f68d2c` | Исправлены основные поверхности светлой темы. |
| `8762fae` | Сброс временного пароля сотрудника из админки. |
| `95c0602` | Восстановлен фон главной страницы. |
| `2872e0c` | Исправлена обработка типа связанного материала. |
| `babbdf3` | Темы, сворачивание разделов, ссылки между материалами. |
| `9de9b9d` | Последовательное открытие тестов стало локальным для раздела. |
| `789989c` | Исправлен запрос ИИ к Yandex-провайдеру. |
| `d8ac1df` | Синхронизация ассортимента и продуктов, исправления футера. |

## Функции, которые уже реализованы и требуют бережного отношения

- Переключатель темной и светлой темы.
- Фон главной страницы до авторизации.
- Сворачивание разделов в обучении и документах.
- Переходы-ссылки между документами, уроками и тестами.
- Редактор уроков и тестов, редактор аттестаций.
- Загрузка нескольких документов.
- Загрузка продуктов из Excel/CSV и экспорт каталога.
- Упрощенная и адаптивная навигация по продуктам.
- Синхронизация дерева ассортимента и категорий продуктов.
- ИИ-помощник с отдельными историями диалогов.
- Исходные роли `u_admin` и `u_staff` без автоматической подмены.
- Сброс временного пароля сотрудника администратором.
- Отправка Web Push и e-mail уведомлений.
- Адаптация встраиваемых плееров для мобильных устройств.
- Политика конфиденциальности как отдельный раздел на странице политик.

## Важные постоянные требования

1. Не удалять и не очищать JSON-данные на сервере.
2. Не смешивать ИИ-историю разных пользователей.
3. Не менять тестовые аккаунты и их роли без прямого указания пользователя.
4. После импорта продуктов данные должны сохраняться после перезагрузки и перехода между разделами.
5. Если приложение аварийно теряет сессию, пользователь должен попадать на главную страницу, а не на обломок защищенного интерфейса.
6. Все новые элементы должны быть адаптивны и не давать тексту выходить за карточки.

## Ожидает отдельного планирования

### Производительность и надежность

- Ускорить загрузку обучения, тестов, документов, сотрудников и продуктов.
- Уменьшить зависания после импорта большого каталога продуктов.
- Добавить быстрый, безопасный клиентский кэш с серверной валидацией данных.
- Проверить накопление логов и размер файлов данных на сервере без их удаления.
- Устранить исчезновение данных документов и уроков, если проблема повторится после проверки API.

### Работа с материалами

- Удобное копирование документа в другой раздел.
- Изменение порядкового номера урока и теста с автоматическим сдвигом остальных карточек.
- Синхронизация ИИ с новой нумерацией.
- Добавить OCR изображений и просмотр старых бинарных DOC/PPT через отдельный конвертер, если это потребуется.

### Продукты и ассортимент

- При появлении новой категории в импортированной таблице автоматически добавлять соответствующую ветку в ассортимент.
- При изменении порядка ассортимента отражать тот же порядок в продуктах.
- Контрольная проверка очистки всех продуктов с обязательным подтверждением.
- Экспорт без колонки даты добавления.

### Поиск, ИИ и уведомления

- Общий поиск должен видеть товары, категории и карточки продуктов.
- ИИ должен полноценно искать сведения по продуктам и всему разрешенному содержимому сайта.
- Ускорить отправку Web Push и e-mail уведомлений.

### Переход на MySQL

- Спроектировать таблицы, индексы, резервное копирование и миграцию JSON-данных.
- Перевести API на слой репозитория, чтобы фронтенд не зависел от формата хранилища.
- Провести миграцию только после резервной копии и теста на отдельной среде.

## Последняя локальная задача: сотрудники

Готово:

1. `/admin#employees` приведен к табличной структуре HTML-прототипа: заголовок, вкладки, поиск, фильтры по точке и роли, прогресс, тесты, статус и действия.
2. Карточка профиля сотрудника приведена к двухколоночной структуре прототипа: краткие сведения, назначенный путь, реальная история попыток и прежнее редактирование учетных данных.
3. Проверены реальные переходы: открытие списка сотрудников и открытие профиля сотрудника в локальном браузере.
4. Статус «Не начал» сделан спокойным красным, а кнопка сброса пароля доступна также для обычного аккаунта администратора. Служебные и системные записи исключены из API и интерфейса.
5. Подпись роли «Администратор» использует точно тот же цвет, что и статус «Не начал».
6. При форме создания сотрудника и профиле прокрутка фоновой страницы заблокирована; у профиля прокручивается только внутреннее окно.
7. В обновленной форме создания сотрудника доступен выбор существующей точки или создание новой. Новая точка сохраняется у учетной записи и появляется в выборе следующих сотрудников.
8. Личный профиль сотрудника и администратора переведен на общий вид профиля из панели сотрудников. Сохранены контакты, аватар, пароль, уведомления, инструкция и выход.
9. В браузере проверен профиль администратора и открытие редактора данных без сохранения.
10. Чтение профиля больше не создает JSON с начальными значениями; API записывает профиль только при явном сохранении формы.
11. `npx tsc --noEmit` и `npm run build` проходят. `npm run build` выводит только известное предупреждение Next.js о двух `package-lock.json`.
12. Во время первой проверки старый API создал неотслеживаемые `data/profile_data_u_admin.json` и `.bak`; они оставлены без изменений, чтобы не нарушать запрет на удаление и перезапись JSON. Коммит, push и деплой не выполнялись.

## Последняя локальная задача: материалы и документы

Готово:

1. Учебные темы и тесты из прежнего пользовательского раздела «Учебные пути» теперь открываются через «Материалы».
2. Прежняя файловая вкладка «Материалы» переименована в «Документы» и вынесена в отдельный пункт боковой панели для администратора и сотрудника.
3. Технические адреса `/tasks?tab=edu` и `/tasks?tab=docs` не изменялись: старые глубокие ссылки, дедлайны и данные остаются совместимыми.
4. Обновлены подписи файловых действий и карточек рабочего пространства, чтобы они называли файловую базу документами.
5. В локальном браузере проверено: «Материалы» открывают темы и тесты, «Документы» открывают загрузку и список файлов.
6. `npx tsc --noEmit` и `npm run build` проходят. Сборка выводит только известное предупреждение Next.js о двух `package-lock.json`.
7. Эта задача не изменяла JSON-файлы в `data/`. Коммит, push и деплой не выполнялись.
8. Администраторский экран материалов объединён с сильными частями HTML-прототипа: сверху расположен центр загрузки и быстрого создания темы или теста вручную либо из документов.
9. Старые разделы, порядок, карточки, перенос, редактирование, удаление, прохождение и AI-черновики сохранены; каталог получил новый визуал, поиск и фильтр по типу материала.
10. «Срочно к прохождению» скрыт у администратора и сохранён в сотрудническом режиме. Администраторский экран, поиск, фильтр, ручной редактор и загрузчик AI проверены в браузере без сохранения данных.
11. После изменения проходят `npx tsc --noEmit`, `npm run build` и `git diff --check`. Коммит, push и деплой не выполнялись.
12. Подписи центра создания упрощены: «Создать самому» и «AI создание», описание страницы коротко упоминает AI, лишний поясняющий абзац удален, а блок разделов получил утвердительный текст и новые названия кнопок.
13. Поиск материалов визуально исправлен: убрана двойная рамка и принудительно сброшены посторонние стили вложенного поля ввода.
14. В истории Git найден исходный набор из 9 готовых тестов. Он вынесен в `app/tasks/data/defaultTrainingTests.ts` и показывается только как резерв при пустом динамическом каталоге, без записи в JSON-файлы `data/`.
15. В production-интерфейсе проверены 25 тем и 9 тестов, поиск «гигиена», фильтр «Только тесты», открытие восстановленного теста с 8 вопросами и закрытие без сохранения.
16. Удалён информационный блок «Черновик остаётся под контролем», поэтому поиск начинается сразу после центра создания материалов.
17. После повторной обратной связи полностью удалён экспериментальный плавный переход темы: View Transition API, временный CSS-класс, таймеры и глобальные переопределения.
18. Переключение темы и сам переключатель возвращены к первой версии, существовавшей до запроса на доработку; удалённый баннер материалов остаётся удалённым.

## Последняя локальная задача: учебные пути

Готово:

1. В администраторской навигации под «Сотрудники» добавлен пункт «Учебные пути» (`/tasks?tab=paths`).
2. Добавлен каталог учебных путей в стиле HTML-прототипа: статус, версия, шаги, источники, число назначений, просмотр и назначение.
3. Пути строятся из реальных разделов тем и тестов, поэтому просмотр показывает фактический состав обучения и открывает выбранную тему или тест.
4. Форма назначения загружает существующих сотрудников, исключая системные и служебные аккаунты.
5. Назначение ограничено текущим сеансом браузера и не записывается в `data/`; это сделано для соблюдения запрета на изменение JSON до согласования отдельной постоянной схемы.
6. Кнопка «Создать путь» открывает рабочую форму: название, версия, описание, статус и выбор тем и тестов. У каждой карточки есть «Редактировать» с текущими значениями и составом пути.
7. В браузере проверены новая навигация, каталог, просмотр структуры и форма назначения без сохранения. `npx tsc --noEmit` и `npm run build` проходят; в сборке есть только известное предупреждение Next.js о двух `package-lock.json`.
8. Коммит, push и деплой не выполнялись.
9. Исправлена компоновка редактора учебного пути: окно не обрезается верхней шапкой, перекрывает фон целиком, имеет внутреннюю прокрутку и доступные кнопки закрытия и сохранения.
10. В редактор добавлен поиск по названию и разделу материалов. Темы и тесты переработаны в кликабельные карточки с иконкой, подписью раздела и отдельным индикатором выбора; фильтрация по запросу «гигиена» проверена в браузере без сохранения. После изменения проходят `npx tsc --noEmit`, `npm run build` и `git diff --check`.
11. Карточки каталога учебных путей визуально переработаны по HTML-эталону: название и версия слева, текстовые метрики снизу, статус и действия справа. На узком экране блоки переходят в вертикальную композицию; компактная кнопка редактирования открывает форму. `npx tsc --noEmit` и `npm run build` проходят.

## Последняя локальная задача: адаптивные темы, тесты и создание материалов

Готово:

1. Все 25 тем и 9 резервных тестов получили единые адаптивные карточки с кратким содержанием, параметрами, статусом и действиями.
2. Просмотр темы полностью переработан по логике HTML-прототипа: полноэкранный материал, боковая информация на компьютере и одноколоночная компоновка на телефоне.
3. Просмотр и прохождение теста получили отдельные адаптивные экраны с правилами, связанными материалами, прогрессом, таймером и крупными вариантами ответа.
4. Ручной редактор темы переработан в блочный конструктор содержания. Редактор теста получил вопросы, варианты ответов, правильный ответ и лимит времени.
5. Верхний центр создания преобразован в «Студию материалов» с отдельными понятными действиями для темы, теста, ручного и AI-сценария.
6. Окно «ВАТЭС AI» визуально переработано для светлой и темной темы: двухколоночная компоновка на компьютере и компактная пошаговая компоновка на мобильном экране.
7. В браузере проверены каталог, открытие темы, ручные редакторы темы и теста, AI-окно и предварительный экран теста. Диалоги закрываются без сохранения и не оставляют блокировку страницы.
8. `npx tsc --noEmit`, `npm run build` и `git diff --check` проходят. `/tasks?tab=edu` отвечает `200` на локальном production-сервере.
9. Отслеживаемые файлы `data/` не изменялись. Коммит, push и деплой не выполнялись.

## Последняя локальная задача: внутренние страницы материалов

Готово:

1. Просмотр темы и теста больше не открывается поверх всего приложения: это внутренний экран раздела с доступной боковой панелью и верхней шапкой.
2. Редакторы темы и теста, прохождение теста и «ВАТЭС AI» переведены на тот же принцип внутренней страницы.
3. Убраны затемненный фон, закрытие кликом вне блока и блокировка прокрутки для внутренних экранов. Блокировка осталась только у настоящих подтверждений, результатов и просмотра изображения.
4. Разные иконки темы заменены новым единым знаком материала в студии, каталоге, просмотре и редакторе.
5. Заголовки боковых информационных карточек собраны в отдельную ровную строку; длинные названия корректно переносятся.
6. Добавлена единая палитра статусов: `is-draft`, `is-ready`, `is-pending`, `is-complete` и `is-blocked`.
7. `npx tsc --noEmit`, `npm run build` и точечный `git diff --check` проходят. Коммит, push и деплой не выполнялись.
8. Исправлено центрирование значков темы и организации библиотеки: общий текстовый CSS больше не переопределяет их flex/grid-контейнеры.
9. Метаданные карточек темы и теста собраны в одну сегментированную строку: количество или формат, время и раздел больше не распадаются на несколько рядов.
10. Завершенные темы и тесты получили единое заметное состояние: зеленоватую поверхность и рамку, усиленную верхнюю полосу и печать «Пройдено» с уточнением «Изучено» или «Тест сдан».
11. Подписи с количеством сохраненных тем и тестов под заголовками каталогов удалены у администратора.
12. Состояние завершения усилено повторно: под обычной шапкой карточки добавлена цельная зеленая плашка «Тема изучена» или «Тест сдан», рамка стала толще, фон и свечение заметнее в обеих темах.

## Последняя локальная задача: пустые разделы материалов

Готово:

1. Создание раздела тем или тестов больше не формирует видимую пустую карточку урока, темы или теста.
2. Технические записи `isPlaceholder` исключены из каталога сотрудника, карточек и счетчиков материалов.
3. Администратор видит сохраненный пустой раздел с нейтральной подсказкой о добавлении материала позже.
4. В браузере проверен существующий пустой раздел: он содержит `0` карточек, показывает одну нейтральную подсказку и не создает безымянный материал. Ошибок в консоли нет.
5. `npx tsc --noEmit`, `npm run build` и `git diff --check` проходят. JSON в `data/` этой задачей не редактировались; коммит, push и деплой не выполнялись.

## Последняя локальная задача: единый визуал каталогов

Готово:

1. Из карточек ручного и AI-создания темы/теста удалены номера `01` и `02`.
2. Карточки тем и тестов зафиксированы по высоте; завершенная карточка больше не увеличивает размер строки каталога.
3. Раздел «Документы» получил новую зону загрузки, заголовок библиотеки, компактные секции и строки файлов.
4. Раздел «Ассортимент» получил отдельную оболочку дерева, счетчики структуры, новые раскрыватели и более ясную вложенность.
5. Раздел «Товары» получил новую панель действий, поиск, одинаковые карточки и компактный блок обязательного ассортимента без пустых зон.
6. Раздел «AI-ассистент» получил заголовок, баннер источников, встроенную историю диалогов, центральный чат и быстрые вопросы; контраст кнопки нового диалога исправлен.
7. В браузере на ширине `1298 px` подтверждены отсутствие `01/02`, одинаковая высота карточек материалов, отсутствие горизонтального переполнения во всех четырех разделах и корректная светлая тема.
8. `git diff --check` и `npm run build` проходят; локальная production-версия отвечает `200` на `http://127.0.0.1:3000`.
9. JSON в `data/` не удалялись и не перезаписывались этой задачей. Коммит, push и деплой не выполнялись.

## Последняя локальная задача: управление всеми разделами

Готово:

1. Действия «Редактировать / Удалить» доступны администратору у всех разделов тем и тестов, включая «Основной раздел».
2. То же правило применено к каждому разделу во вкладке «Документы».
3. Переименование использует существующее окно с текущим названием; удаление по-прежнему требует подтверждения и предупреждает об удалении всего содержимого раздела.
4. В браузере открыты и отменены окна переименования и удаления основного раздела без сохранения данных.
5. `npm run build` и `git diff --check` проходят; локальный сервер отвечает `200`. JSON не изменялись, коммит, push и деплой не выполнялись.

## Последняя локальная задача: шапка завершенных материалов

Готово:

1. Завершенная тема больше не заменяет обычную верхнюю строку карточки зеленой плашкой.
2. У пройденной темы видны тип, номер урока и все три действия администратора: перемещение, редактирование и удаление.
3. Статус прохождения перенесен в компактную строку под шапкой; нижний статус сокращен до «Изучено». Для завершенных тестов действует тот же шаблон со статусом «Сдан».
4. Завершенные и обычные карточки сохраняют одинаковую высоту `340px`; внутренняя область не переполняется. Светлая и темная темы проверены в локальном браузере.
5. `npm run build` и `git diff --check` проходят, `/tasks?tab=edu` отвечает `200`. JSON в `data/` не удалялись и не перезаписывались; коммит, push и деплой не выполнялись.

## Последняя локальная задача: правила перед началом теста

Готово:

1. Из трех карточек правил предпросмотра теста удалены декоративные номера `01`, `02` и `03`.
2. Пояснения сокращены без потери смысла: переход к следующему вопросу, один правильный ответ и порог `80%`.
3. Карточки уплотнены до одинаковой высоты около `86px`; лишний верхний отступ удален, текст не переполняется.
4. Светлая и темная темы проверены в локальном браузере. `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=edu` подтверждены.
5. JSON в `data/` не удалялись и не перезаписывались; коммит, push и деплой не выполнялись.

## Последняя локальная задача: возврат к материалам

Готово:

1. В `CustomIcon.tsx` добавлена единая SVG-иконка `arrow-left`. Все пять кнопок класса `.vates-material-back-button` в просмотре темы, редакторах, просмотре теста и тестовой сессии используют стрелку вместо крестика.
2. `Education.tsx` сохраняет `window.scrollY` непосредственно перед открытием карточки, ручного редактора или AI-экрана. Это происходит до скрытия длинного каталога, поэтому координата не обрезается короткой внутренней страницей.
3. После закрытия внутреннего экрана восстановление ожидает, пока каталог снова достигнет нужной высоты, и только затем возвращает сохраненную позицию.
4. Очистка параметров `routeId/testId` выполняется через `router.replace(..., { scroll: false })`, поэтому Next.js больше не сбрасывает восстановленную позицию.
5. В браузере проверены нижняя тема «Открытие смены» и нижний тест «Напитки на вынос и допродажа»: после нажатия «К материалам» обе исходные карточки остаются в видимой области. В редакторе теста также подтверждены стрелка и возврат к исходной карточке.
6. `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=edu` проходят. JSON в `data/` не удалялись и не перезаписывались; коммит, push и деплой не выполнялись.

## Последняя локальная задача: пустая полоска в тесте

Готово:

1. Найден пустой внешний контейнер списка связанных материалов: внутренний компонент возвращал `null`, но родитель продолжал рисовать рамку.
2. Пустой `.vates-material-linked-section` теперь полностью скрывается через `:empty`; правило действует и в тестах, и в темах.
3. Нижняя панель действий поднялась к карточкам правил, между ними осталось `24px`; пустой контейнер имеет высоту `0`.
4. Исправление проверено в локальном браузере. `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=edu` подтверждены.
5. JSON в `data/` не удалялись и не перезаписывались; коммит, push и деплой не выполнялись.

## Последняя локальная задача: шапка библиотеки документов

Готово:

1. Блок «Проверяйте документы перед публикацией» полностью удален из `Documents.tsx` без пустого отступа.
2. Кнопка «Новый раздел» больше не смешивает старые inline-стили с новым компонентным классом. Добавлены иконка папки, белый текст, контрастный фон, размер `166×42px` и полноширинное мобильное состояние.
3. Подсказка пустой библиотеки теперь ссылается на фактическое название кнопки «Новый раздел».
4. В браузере подтверждены отсутствие предупреждения и читаемость кнопки в светлой и темной темах. `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=docs` проходят.
5. JSON в `data/` не удалялись и не перезаписывались; коммит, push и деплой не выполнялись.

## Последняя локальная задача: обновление страницы товаров

Готово:

1. Шапка «Товаров» получила ясную иерархию действий: экспорт, импорт, создание товара и очистка каталога визуально разделены по назначению.
2. Добавлена сводка по каталогу: общее число товаров, разделов, обязательных и скрытых позиций.
3. Поиск объединен с текущим разделом и живым счетчиком найденных товаров; дерево категорий стало компактнее и лучше показывает выбранный путь.
4. Карточки товаров переработаны без изменения обработчиков: показаны категория, описание, код, приоритет, доступность и действие открытия; все карточки уменьшены до одинаковой высоты `286px`.
5. Блок обязательного ассортимента получил компактный заголовок. Счетчик вынесен вправо в отдельный индикатор «N / в подборке», а активный значок обязательного товара повторяет бирюзовый цвет блока в светлой теме и теплый предупреждающий цвет в темной. Повтор кода и приоритета в нижнем статусе удален.
6. Поисковое поле исправлено: оно занимает всю доступную ширину, имеет собственную поверхность, ровную иконку, рамку и состояние фокуса.
7. В локальном браузере проверены светлая и темная темы, девять карточек размером `283×286px` без внутреннего переполнения, поиск шириной `882px`, отдельное расположение счетчика, совпадение цветов активного значка и фокус-блока и отсутствие горизонтального переполнения при ширине `1440px`. Ошибок в консоли нет.
8. `npm run build` и `git diff --check` проходят; локальный production-сервер отвечает `200` на `/tasks?tab=products`.
9. JSON в `data/` не удалялись и не перезаписывались. Коммит, push и деплой не выполнялись.

## Последняя локальная задача: удобство AI-ассистента

Готово:

1. Чат сокращен до двух рабочих областей: компактная история и большое поле переписки. Отдельная колонка быстрых вопросов отсутствует.
2. Полностью удалены приветственная карточка «Чем помочь сейчас?», быстрые сценарии, верхний статус подключения базы и повторный статус в панели диалога.
3. Верхняя панель чата уменьшена до названия текущего диалога; на мобильном экране рядом остается только кнопка открытия истории.
4. Пустая история больше не растягивается на высоту боковой панели: она показывает короткую текстовую подсказку без декоративной карточки, иконки и числового счетчика справа от заголовка. Поиск скрыт, пока диалогов нет.
5. Композер собран заново как единая нижняя строка без служебных подписей. Добавлен отдельный SVG бумажного самолёта и текстовая кнопка «Отправить», которая становится фирменно-бирюзовой после ввода текста.
6. Поле сообщений использует спокойный однотонный фон без сетки, лишних рамок и стартового содержимого. На экране `1298×670` под сообщения доступно `316px`, вся рабочая область и композер полностью видимы.
7. На ширине `390px` поле сообщений имеет высоту `392px`, композер полностью находится выше мобильной навигации, история открывается сбоку и горизонтального переполнения нет.
8. В браузере проверены светлая и темная темы, активное состояние кнопки отправки и мобильная панель истории без отправки запроса в AI.
9. `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=standards` проходят.
10. JSON в `data/` не удалялись и не перезаписывались. Коммит, push и деплой не выполнялись.

## Последняя локальная задача: компактная шапка AI-ассистента

Готово:

1. Высота шапки AI-раздела уменьшена с `98px` до `86px`: сокращены размер заголовка и вертикальные отступы описания.
2. Рабочая область истории и чата увеличена с `450px` до `466px` на экране `1298×670` без смещения нижней границы страницы.
3. В браузере подтверждены отсутствие горизонтального переполнения и полная видимость композера; `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=standards` проходят.
4. JSON в `data/` не удалялись и не перезаписывались. Коммит, push и деплой не выполнялись.

## Последняя локальная задача: результаты тестов и футер

Готово:

1. Из футера удалены начальный, внутренний и конечный символы `|`; в авторской строке вертикальный разделитель заменен на точку.
2. Окно результатов тестов переведено со старых темных inline-стилей на адаптивные классы светлой и темной темы.
3. Добавлены фильтр сотрудников, сводка по результатам, числу пройденных тестов и среднему баллу, компактные карточки попыток и SVG-кнопка закрытия.
4. В браузере проверены обе темы на ширине `870px`: три карточки помещаются без переполнения, подпись «Средний балл» не обрезается, футер содержит две ссылки и `0` вертикальных палочек.
5. `npm run build`, `git diff --check` и ответ `200` от `/admin` проходят. JSON в `data/` не удалялись и не перезаписывались; коммит, push и деплой не выполнялись.

## Последняя локальная задача: компактные каталоги и страница товара

Готово:

1. Кнопки карточек «Учебных путей» переведены с абсолютного позиционирования на адаптивную сетку. Все три действия имеют одинаковую высоту `44px`, полные подписи и ровные интервалы.
2. Все карточки тем и тестов уменьшены с `340px` до единой высоты `272px`. Уплотнены шапка, статус завершения, описание, метаданные и нижнее действие без потери административных кнопок.
3. Строки документов уменьшены до минимальной высоты `92px`; размер и кнопки выровнены с названием, а на мобильной ширине снова собираются в колонку.
4. Корневые и вложенные узлы «Ассортимента» сортируются по числовому префиксу. Та же сортировка применяется к дереву разделов «Товаров» без изменения исходной структуры JSON.
5. Блок обязательных товаров уменьшен до карточек высотой `210px`. На широком экране рассчитано три позиции в строке, до `1180px` две, на мобильном экране одна.
6. Карточки товарного каталога уменьшены до одинаковой высоты `258px`. Открытие товара теперь ведет на отдельный внутренний экран с описанием, путем каталога, параметрами и административными действиями; модальный фон больше не используется.
7. Кнопка «К товарам» возвращает каталог к сохраненной вертикальной позиции. Боковая и верхняя навигация остаются доступными на странице товара.
8. В локальном браузере проверены темная и светлая темы, порядок разделов `1–8`, одинаковые размеры карточек, отсутствие горизонтального переполнения и возврат к позиции каталога. Свежих ошибок консоли после перезагрузки нет.
9. `npx tsc --noEmit`, `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=products` проходят. JSON в `data/` не удалялись и не перезаписывались; коммит, push и деплой не выполнялись.

## Выполнено: безопасный релиз редизайна на GitHub и хост

- [x] Проверен состав изменений и исключены `data/*.json`, резервные копии, `.env*` и секреты.
- [x] Локально пройдены `npx tsc --noEmit`, `npm run build` и `git diff --check`.
- [x] Коммит `cc4b36f Refresh Vates learning and admin workspace` отправлен в `origin/main`.
- [x] До обновления создан архив `/var/backups/tea-hub/20260719-022540/data.tar.gz`, список файлов и SHA-256-манифест.
- [x] На хосте выполнен `git pull --ff-only`, затем успешные `npm run build` и `pm2 restart tea-hub`; процесс `tea-hub` работает.
- [x] Все 144 файла `data/` совпали по контрольным суммам до и после обновления; 122 JSON-файла корректно разбираются.
- [x] В публичной авторизованной сессии проверены админ-панель, сотрудники, материалы, документы и товары. Старые данные отображаются в новом интерфейсе без миграции и потерь.

## Выполнено: мобильный футер

- [x] Ссылки на политику и cookie переведены в адаптивную сетку с одинаковой высотой строк.
- [x] Подпись поддержки и email разделены на мобильном экране; адрес больше не обрезается справа и открывает почтовое приложение через `mailto:`.
- [x] Защищены переносы копирайта и служебных строк на узких устройствах.
- [x] Проверены ширины `320`, `360` и `390px`: горизонтального переполнения и ошибок консоли нет.
- [x] Пройдены `npx tsc --noEmit`, `npm run build` и `git diff --check`.
- JSON в `data/` не удалялись и не перезаписывались. Коммит, push и деплой не выполнялись.

## Выполнено: единый визуал раздела «Документы»

- [x] Центр загрузки документов приведен к композиции студии материалов.
- [x] Добавлены единые поиск, фильтр по разделу и счетчик найденных документов.
- [x] Секции и документы переведены из длинных строк в компактную адаптивную карточную сетку.
- [x] Сохранены прежние действия загрузки, открытия, скачивания, перемещения, копирования и удаления.
- [x] Проверены `1280×720` и `390×844`: сетка перестраивается `3 → 1`, горизонтального переполнения нет.
- [x] Пройдены `npx tsc --noEmit`, `npm run build` и `git diff --check`.
- JSON в `data/` не удалялись и не перезаписывались. Коммит, push и деплой не выполнялись.

## Выполнено: компактная карточка входа

- [x] Удален дублирующий логотип внутри формы входа; основной логотип в сюжетной части сохранен.
- [x] Добавлены компактный заголовок, пояснение и ровные вертикальные отступы без пустой верхней зоны.
- [x] Компактные размеры изолированы от формы регистрации отдельными классами.
- [x] Локально проверены рабочий экран `1280×720` и мобильный экран `390×844`: горизонтального переполнения и ошибок консоли нет.
- [x] Пройдены `npx tsc --noEmit`, `npm run build` и `git diff --check`.
- JSON в `data/` не удалялись и не перезаписывались. Коммит, push и деплой не выполнялись.

## Выполнено локально: мобильный предпросмотр документов

- [x] DOCX переведен в управляемый внутренний предпросмотр с доступной кнопкой закрытия.
- [x] На мобильном экране закреплены верхняя панель и нижние действия, а документ прокручивается внутри доступной области.
- [x] Добавлены ограничения ширины для DOCX-страниц, изображений и таблиц без горизонтального смещения.
- [x] Резервная отдельная вкладка получила верхнюю панель со стрелкой и возвратом в раздел документов.
- [x] Добавлено закрытие по `Escape` и блокировка прокрутки фоновой страницы.
- [x] На ширине `390×844` подтверждены видимая кнопка закрытия, полная высота окна и отсутствие горизонтального переполнения.
- [x] Пройдены `npx tsc --noEmit`, `npm run build` и `git diff --check`; локальная версия отвечает `200` на `http://127.0.0.1:3000/login`.
- Локальный production-сервер оставлен запущенным на `http://127.0.0.1:3000`. Новое исправление не закоммичено, не отправлено и не развернуто.
- Существующий пользовательский `data/tea_hub_dynamic_route_v2.json` не изменялся и не должен входить в будущий коммит без отдельной команды.

## Выполнено локально: центрирование значков редакторов

- [x] Найден конфликт CSS, который заменял `inline-flex` контейнера значка на `block`.
- [x] Текстовый селектор ограничен подписями заголовка и больше не затрагивает контейнер `vates-editor-card-icon`.
- [x] Для внутренней обертки и SVG закреплены одинаковые размеры и центрирование.
- [x] В браузере проверены ручный редактор темы и ручный редактор теста: центры квадрата, обертки и SVG совпадают.
- [x] Пройдены `npx tsc --noEmit`, `npm run build` и `git diff --check`; локальная версия снова запущена на `http://127.0.0.1:3000`.
- JSON в `data/` не изменялись. Commit, push и deploy не выполнялись.

## Выполнено локально: экран обучения сотрудника

- [x] Белый цвет заголовков и пояснений закреплен для блоков общего прогресса и текущего учебного пути в обеих темах.
- [x] Одиночные карточки тем, тестов, документов и срочных дедлайнов сохраняют ширину одной колонки.
- [x] Для этих каталогов закреплена адаптивная сетка `3 → 2 → 1`.
- [x] В браузере подтверждены белый цвет обоих текстов и ширина одиночных карточек `356–357px` внутри контейнера `1100px`.
- [x] Пройдены `npx tsc --noEmit`, `npm run build` и `git diff --check`; локальная версия отвечает `200`.
- JSON в `data/` не изменялись. Commit, push и deploy не выполнялись.

## Выполнено локально: полноэкранный вход с футером

- [x] Общий футер возвращён на страницу входа и больше не скрывается отдельным правилом.
- [x] Основная область входа и футер объединены в сетку высотой `100dvh`, поэтому страница не имеет вертикальной прокрутки.
- [x] Сюжетная часть и карточка входа разделены без наложения; весь текст виден на мобильном и компьютерном экранах.
- [x] При переходе к длинной форме регистрации полноэкранное ограничение снимается, обычная прокрутка возвращается.
- [x] В браузере проверены `390×844`, `726×790` и `1440×900`: высота документа равна высоте окна, футер полностью виден.
- [x] Пройдены `npx tsc --noEmit`, `npm run build` и `git diff --check`; локальная версия запущена на `http://127.0.0.1:3000`.
- JSON в `data/` не изменялись. Commit, push и deploy не выполнялись.
