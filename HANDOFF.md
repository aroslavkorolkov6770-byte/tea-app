# Передача проекта в новый чат

## Готовый текст для первого сообщения новому Codex

```text
Продолжай проект Ватэс из папки C:\Users\KROLL\Desktop\tea-app-main.

Сначала прочитай AGENTS.md, PROJECT_CONTEXT.md, TASKS.md и этот HANDOFF.md. Не удаляй и не перезаписывай JSON-файлы в data/. Не используй git reset --hard, git clean или команды очистки данных.

В рабочей папке находится локальный полный редизайн по прототипу Vates. Основная тема светлая, переключатель включает темную тему. В боковом меню обязательно сохранить отдельные пункты «Ассортимент» и «Товары». Не выполнять commit, `git fetch`, `git pull`, `git push` или деплой, пока пользователь не даст прямую команду.

Локально удалена автоподмена `u_staff` в системного администратора. Исходный `u_admin` остается администратором, `u_staff` остается сотрудником. Обычное чтение пользователей больше не переписывает JSON и не создает `.bak`. Это изменение еще не опубликовано.

Не размещай в коде, документации или ответах пароли, API-ключи, VAPID-ключи и SSH-данные. ИИ-истории должны оставаться изолированными по ID пользователей.
```

## Состояние на момент передачи

- Проект открыт в рабочей копии `C:\Users\KROLL\Desktop\tea-app-main`.
- Ветка: `main`; базовый коммит локального редизайна: `a8a9a4d`.
- Редизайн Ватэс еще не опубликован в GitHub и не развернут на хосте.
- Текущая production-сборка запущена на `http://localhost:3000/`. Оригинальный HTML-прототип из ZIP отдельно запущен на `http://localhost:3200/`.
- В браузере был выполнен вход под тестовой административной учетной записью для визуального аудита. Не использовать и не выводить ее пароль.
- JSON-файлы не являются частью редизайна. После исправления `serverAuth.ts` авторизованное открытие `/admin` не изменяет `data/`; контроль `git diff --exit-code -- data` проходит.

## Локальный редизайн Ватэс

- Добавлены `app/components/VatesLogo.tsx`, оригинальные логотипы в `public/` и новая брендовая оболочка в `app/globals.css`.
- Обновлены навигация, темы, вход, рабочее пространство, сотрудники, профиль сотрудника, прогресс, учебные пути, урок, тест, результат, материалы, просмотр документов, ассортимент, товары, AI, личный профиль, футер, cookie-баннер, manifest и service worker.
- Светлая тема загружается по умолчанию. Выбор темной темы хранится отдельно в ключе `vates_theme_v1`.
- Переключатель темы на странице входа больше не расположен на стыке двух колонок: на десктопе он находится в верхнем правом углу рабочей части рядом с переходом на главную, а на мобильном экране остается справа сверху в брендовой зоне.
- Карточка входа не имеет собственного скролла и ограничения высоты: длинная форма регистрации увеличивает документ, поэтому пользователь прокручивает обычную страницу браузера. Ссылка регистрации начинается с «Первый вход?».
- Дополнительная подпись «Обучение и знания команды» над основным заголовком брендовой части страницы входа удалена.
- Обычная форма входа и компактный общий футер помещаются в один экран без вертикальной прокрутки на компьютере и телефоне. Весь сюжетный текст остаётся видимым; при переходе к длинной регистрации обычная прокрутка страницы возвращается.
- В десктопной боковой панели доступны рабочее пространство, сотрудники, учебные пути, материалы, ассортимент, товары и AI-ассистент. Панель имеет ширину `248px`, до `1100px` сжимается до `84px` и оставляет только SVG-иконки.
- Логотип боковой панели также адаптирован: на широком экране показывается знак с названием, а в компактной панели до `1100px` остаётся только чистый знак без обрезанного текста.
- До `767px` боковая панель полностью скрыта, а основные разделы доступны через нижнюю навигацию. Плавающая кнопка меню удалена.
- `/admin` показывает рабочее пространство, а `/admin#employees` - отдельный экран сотрудников только с управлением учетными записями. Коммуникации, аттестации, календарь, заметки и дедлайны находятся в `/admin`.
- В блоке оперативной работы отправка и календарь выровнены в верхнем ряду, а «Ближайшие события» с вкладками «Заметки / Дедлайны» занимает всю ширину ниже. Декоративные квадратные иконки заголовков удалены.
- Карточки событий имеют фиксированную увеличенную высоту, отображают день, месяц и год и не вводят в заблуждение при заметках из разных месяцев. По нажатию открывается полное содержание в модальном окне, без записи в JSON.
- Форма личной заметки в календаре разделена на заголовок и подробности с видимыми счетчиками `120` и `2000` символов. Панель «Ближайшие события» без декоративной иконки в пустом состоянии имеет одну начальную высоту для заметок и дедлайнов и увеличивается только при большом числе карточек.
- Карточка «Требует внимания» показывает только реальные проблемы и корректное спокойное состояние без нулевых строк. Круговой прогресс анимируется только если значение больше нуля и повторяет заполнение при выборе периода `7`, `30` или `90` дней.
- Последняя локальная проверка: `npx tsc --noEmit`, `npm run build` и `git diff --check` проходят, `/admin` отвечает `200`. В браузере проверены поля и счетчики личной заметки, их обновление при вводе, а также пустая вкладка дедлайнов; тестовый ввод закрыт без сохранения. Отслеживаемые файлы `data/` не изменены.
- Вкладки `/admin` и `/admin#employees` намеренно используют обычные ссылки: `Next Link` не удалял hash при переходе на тот же маршрут. Переходы в обе стороны проверены реальным кликом.
- AI называется `Ватэс AI`; визуальные эмодзи заменены компонентом `CustomIcon`.
- Проверены рабочее пространство, сотрудники, обучение, материалы, товары и AI в светлой теме; рабочее пространство проверено в темной теме. Футер прошлой версии восстановлен полностью.
- Перед будущей публикацией повторить `git diff --check`, `npx tsc --noEmit`, `npm run build` и контроль `data/`.

## Развернутый просмотр документов

Изменение опубликовано в рабочей ветке, `main` и на сервере:

- `app/lib/documentPreview.ts` определяет тип просмотра, декодирует UTF-8, UTF-16, Windows-1251/1252 и отличает текст от известных бинарных форматов;
- `app/api/document-preview/route.ts` требует сессию и извлекает для просмотра текст из офисных документов без записи в JSON-хранилище;
- `app/lib/documentTextExtractor.ts` использует общий декодер, поддерживает неизвестные текстовые расширения и отдельные лимиты просмотра;
- `app/tasks/components/Documents.tsx` показывает текст в безопасном прокручиваемом блоке как при обычном открытии, так и при переходе из дедлайна;
- `app/globals.css` содержит адаптивные стили встроенного текстового просмотра.

Проверено локально: неизвестное расширение UTF-8, UTF-16, Windows-1251 и XLSX вернули правильный текст через `/api/document-preview`; существующий `template_products (1).csv` открылся в реальном интерфейсе как читаемый текст. `npx tsc --noEmit`, точечный ESLint новых файлов и `npm run build` проходят. Общий ESLint старого `Documents.tsx` по-прежнему показывает накопленные ошибки `any` и правил React. Тестовый production-сервер использовал порт 3100 и после проверки остановлен.

Релиз `464a0c0` развернут в `/var/www/html`: серверная production-сборка прошла, PM2-процесс `tea-hub` находится в статусе `online`, главная страница отвечает `200`. На публичном домене авторизованный маршрут `/api/document-preview` прочитал UTF-8-файл с неизвестным расширением и вернул правильный текст; запрос без сессии возвращает `401`.

## Развернутое исправление дедлайнов

Изменения в `app/admin/page.tsx` и `app/tasks/components/Education.tsx` опубликованы:

- администратор может прикрепить к дедлайну документ, тему или тест;
- новая ссылка записывается в `linkedMaterial`, для тестов также сохраняется старый `linkedTestId`;
- старые дедлайны остаются совместимыми;
- документ открывается через `documentId`, тема через `routeId`, тест через прежний алгоритм блокировок;
- поиск нормализует идентификаторы и названия, а ошибка сохранения оставляет форму открытой.

Коммит `31c25cf` отправлен в рабочую ветку и `main`, сервер `/var/www/html` обновлен fast-forward. Локальная и серверная production-сборки прошли, PM2-процесс `tea-hub` находится в статусе `online`. На публичной странице администратора без сохранения реального дедлайна проверено наличие выбора документа, темы и теста; ошибок браузера нет. Точечный ESLint двух старых крупных файлов по-прежнему падает на их накопленном lint-долге.

После публикации пользователь обнаружил, что выбранный материал не отправляется, если поле `Текст задачи` пустое. Серверные данные подтвердили: выполнялось только сохранение календаря, а задача не создавалась. В коммите `21f174b` ранний выход исправлен: материал автоматически формирует текст дедлайна, а полностью пустая форма показывает ошибку. Контрольная локальная отправка с документом создала обе записи и корректную ссылку; тестовые данные восстановлены. Исправление отправлено в рабочую ветку и `main`, развернуто на сервере, сборка прошла, PM2 находится в статусе `online`.

## Состав реализованного релиза

### Сервер

- `app/api/ai-content-draft/route.ts` - администраторский multipart API, лимиты файлов, запрос Alice AI, разбор и проверка JSON-черновика.
- `app/lib/aliceAi.ts` - общий серверный клиент Alice AI и извлечение текста ответа.
- `app/lib/documentTextExtractor.ts` - извлечение текста из PDF, DOCX, таблиц, презентаций, OpenDocument, RTF и текстовых файлов.
- `package.json`, `package-lock.json` - добавлены `mammoth`, `pdf-parse`, `jszip`.

### Интерфейс

- `app/tasks/components/AiContentGenerator.tsx` - двухшаговое окно загрузки и настройки, отдельные светлые и темные стили.
- `app/tasks/components/Education.tsx` - меню ручного/AI-создания и перенос черновика в существующие редакторы темы и теста.
- AI не сохраняет материал автоматически. Сохранение выполняется только кнопкой администратора после проверки.

### Документация

- `AGENTS.md` требует обновлять `PROJECT_CONTEXT.md`, `TASKS.md` и `HANDOFF.md` после каждого завершенного изменения кода.
- Текущий контекст и статус AI-генератора записаны в Markdown-файлы проекта.

## Выполненная проверка

1. `npx tsc --noEmit` проходит.
2. `npm run build` проходит, маршрут `/api/ai-content-draft` включен в сборку.
3. Новые отдельные файлы проходят точечный ESLint.
4. Полный ESLint `Education.tsx` показывает старые накопленные `any` и другие ошибки, существовавшие до этой задачи.
5. Окно генератора проверено в локальном браузере в светлой и темной темах.
6. Авторизация API, прием Markdown-файла и извлечение текста проходят. Локальный запрос останавливается только на ожидаемом отсутствии `AI_API_KEY`.
7. На публичном хосте Alice AI вернула корректную тему из двух блоков без предупреждений.
8. Опубликованное окно генератора проверено визуально на `tea-hub.ru/tasks?tab=edu`.

## Проверка после продолжения

1. В окружении с настроенной Alice AI создать тест из короткого TXT или DOCX.
2. Проверить, что тест открылся в редакторе, но не появился в списке до ручного сохранения.
3. Проверить правильные ответы у всех вопросов и только затем сохранить.
4. Повторить для темы из PDF или DOCX.
5. Проверить мобильное окно на ширине около 430 пикселей.
6. Выполнить `git diff --check`, `npx tsc --noEmit` и `npm run build`.
7. Только после прямой команды пользователя создать коммит и выполнить деплой.

## Известный технический долг

`npm run lint` падает на накопленных ошибках старого `Education.tsx`; новые самостоятельные файлы проходят точечную проверку. Локальный dev-процесс не содержит `AI_API_KEY`, но полная генерация успешно проверена через настроенный публичный хост.

## Безопасная заливка при будущей команде пользователя

```powershell
git status --short
git diff --check
git add <только файлы кода>
git commit -m "Короткое понятное описание"
git push
```

После GitHub на сервере выполняется обычное обновление кода, зависимостей, сборка и перезапуск процесса. Точный путь текущего развертывания: `/var/www/html`. Последовательность на сервере: `cd /var/www/html`, затем `git pull --ff-only`, `npm install` при изменении зависимостей, `npm run build`, `pm2 restart tea-hub`. Перед этим нужно проверить, что команды не затронут `data/` и другие пользовательские файлы. Никогда не очищать данные и не выполнять разрушительные команды Git.

## Текущее продолжение

- Локально обновлены `app/admin/components/UserManagement.tsx`, `app/admin/components/UserProfileModal.tsx` и `app/globals.css`.
- `/admin#employees` теперь визуально следует HTML-прототипу: таблица сотрудников с точкой, ролью, прогрессом, тестами, статусом и рабочими действиями.
- Профиль сотрудника открывается в обновленной компоновке: карточка данных, назначенный путь, реальные результаты обучения и прежнее редактирование доступа.
- Статус «Не начал» выделен красным, сброс временного пароля доступен обычным сотрудникам и администраторам, но недоступен системным и служебным аккаунтам.
- Роль «Администратор» использует точно тот же красный цвет, что и статус «Не начал».
- Фон не прокручивается при открытом профиле, форме создания или диалоге сброса пароля; профиль сохраняет внутреннюю прокрутку.
- Форма создания сотрудника переработана в стиль Ватэс: выбор существующей точки, пункт создания новой точки и поле ее названия. Точки не имеют отдельного JSON-справочника: значение хранится у созданного сотрудника и становится следующим вариантом выбора.
- Личный путь `/profile` для администратора и сотрудника теперь повторяет структуру профиля из панели сотрудников: карточка пользователя, путь, авторизация, контакты, результаты, прогресс и уведомления. Действия личного профиля сохранены.
- `/api/account/profile` больше не пишет начальные данные при простом чтении. Во время первой проверки старый код успел создать неотслеживаемые `data/profile_data_u_admin.json` и `.bak`; они не удалялись и не изменялись.
- Проверены `npx tsc --noEmit`, `npm run build`, список и профиль в браузере. Коммита, push и деплоя нет.

## Текущее продолжение: навигация материалов

- «Материалы» в боковой панели теперь открывают учебные темы и тесты через совместимый адрес `/tasks?tab=edu`.
- Бывшая файловая вкладка «Материалы» переименована в «Документы» и вынесена в новый пункт боковой панели `/tasks?tab=docs` для обеих ролей.
- В интерфейсе обновлены связанные тексты файловой базы и карточек рабочего пространства. Данные, файлы, темы и тесты не переносились, JSON в `data/` этой задачей не менялся.
- Локально проверены обе вкладки в браузере, `npx tsc --noEmit` и `npm run build` проходят. Коммита, push и деплоя нет.
- Администраторские «Материалы» получили верхний центр создания: пустая тема, тема из документов, пустой тест, тест из документов и создание разделов. Эти кнопки используют существующие формы и AI-генератор.
- Ниже сохранён прежний каталог по разделам и карточкам, но он визуально обновлён и дополнен поиском с фильтром по темам и тестам. «Срочно к прохождению» теперь существует только в режиме сотрудника.
- В браузере проверены отсутствие срочного блока у администратора, фильтрация по запросу «гигиена», переключение типа, ручной редактор и загрузчик AI без сохранения. `npx tsc --noEmit`, `npm run build` и `git diff --check` проходят.
- Центр создания теперь использует короткие действия «Создать самому» и «AI создание»; описание разделов стало утвердительным, а лишний абзац под заголовком центра удален.
- Исправлена двойная рамка строки поиска. При пустом сохраненном каталоге показываются 9 готовых тестов, восстановленных из старой версии проекта в `app/tasks/data/defaultTrainingTests.ts`; резерв не записывается в `data/`, а непустые сохраненные тесты всегда имеют приоритет.
- После production-сборки в браузере подтверждены 25 тем, 9 тестов, поиск и фильтр; восстановленный тест открылся с вопросами и был закрыт без сохранения.
- Баннер «Черновик остаётся под контролем» удалён. Эксперимент с плавной сменой темы полностью отменён: `ThemeProvider` и CSS переключателя возвращены к первой версии до этой доработки.

## Текущее продолжение: учебные пути

- В администраторской боковой панели после «Сотрудники» добавлены «Учебные пути» (`/tasks?tab=paths`).
- Новый экран использует существующие разделы тем и тестов как пути: показывает карточки, шаги, источники, просмотр состава и форму выбора сотрудника.
- Переход из состава пути открывает исходную тему или тест в «Материалах». Форма назначения исключает системные учетные записи.
- Создание и редактирование пути выполняются через форму названия, версии, описания, статуса и выбора тем и тестов. Назначение, новые пути и изменения намеренно сохраняются только в текущем сеансе браузера, а постоянное хранение не добавлялось, чтобы не менять JSON в `data/`.
- Локально проверены навигация, каталог, просмотр и форма назначения. `npx tsc --noEmit` и `npm run build` проходят; коммита, push и деплоя нет.
- Редактор путей исправлен после визуального замечания: он выше боковой панели и шапки, имеет правильную высоту с учетом отступов, внутреннюю прокрутку и закрепленные заголовок с действиями.
- В редакторе появился поиск по названию и разделу тем и тестов. Элементы состава стали отдельными выбираемыми карточками с иконкой, названием, разделом и индикатором выбора; поиск «гигиена» в браузере оставляет только нужную тему. После изменения проходят `npx tsc --noEmit`, `npm run build` и `git diff --check`; отслеживаемые JSON в `data/` не изменены.
- Карточки каталога путей стилистически сближены с HTML-эталоном: крупные название и версия, спокойная строка показателей, статус с цветной точкой и единый правый блок действий. На мобильной ширине карточка перестраивается без переполнения; компактный карандаш открывает редактор и имеет доступную подпись.

## Текущее продолжение: адаптивные темы и тесты

- В `app/tasks/components/Education.tsx` переработаны карточки всех тем и тестов, полноэкранные просмотры, прохождение теста и ручные редакторы темы и теста.
- В `app/tasks/components/AiContentGenerator.tsx` обновлено окно «ВАТЭС AI»: источники и параметры разделены на два понятных шага, предусмотрена мобильная компоновка.
- В `app/globals.css` добавлена единая адаптивная система для студии материалов, карточек, полноэкранных рабочих пространств, редакторов и тестовой сессии.
- На компьютере просмотр и редактирование используют основную область и боковой инспектор; на телефоне все блоки переходят в одну колонку, а действия остаются доступными внизу экрана.
- Локально проверены 25 тем, 9 тестов, просмотр темы, редакторы темы и теста, AI-окно и предварительный экран теста. Сохранение во время проверки не выполнялось.
- `npx tsc --noEmit`, `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=edu` подтверждены. Отслеживаемые JSON в `data/` не изменены.
- Локальный production-сервер запущен по адресу `http://127.0.0.1:3000`. Коммита, push и деплоя нет.

## Текущее продолжение: внутренние страницы материалов

- Тема, тест, ручные редакторы, тестовая сессия и `AiContentGenerator` теперь рендерятся внутри `Education`, а не фиксированным оверлеем поверх всего приложения.
- Класс `is-material-subpage-open` скрывает только каталог материалов. Боковая навигация, верхняя панель и общий каркас `/tasks` остаются видимыми.
- Прокрутка `body` блокируется только для настоящих модальных подтверждений, результатов, срочных тестов и просмотра изображения.
- В `CustomIcon` добавлен единый знак `material`; старые иконки книги, файла и ракеты в теме заменены им.
- Боковые карточки используют `vates-material-inspector-heading`, а статусы приведены к общей палитре `draft / ready / pending / complete / blocked`.
- Production-сборка и TypeScript проходят. После перезапуска локального production-сервера прежняя браузерная авторизация перестала отображать защищенный экран, поэтому повторная визуальная проверка потребует обычного входа пользователя; учетные данные агент не извлекал и не вводил.
- JSON в `data/` этой задачей не изменялись. Коммита, push и деплоя нет.
- Значки в `.vates-material-card-type-icon` и `.vates-material-studio-footer-icon` центрируются отдельными специфичными CSS-правилами; это защищает их от общих правил `span { display: block; }` внутри карточек.
- `.vates-material-card-meta` теперь является трехсегментной строкой без переноса: два коротких параметра занимают ширину по содержимому, раздел использует оставшееся место с безопасным многоточием.
- Завершенные карточки тем и тестов используют общий класс `is-complete` с зеленой поверхностью. Нижний компактный статус различается текстом «Изучено» или «Сдан».
- Администраторские счетчики «тем/тестов в сохраненных разделах» под заголовками каталогов удалены; сотруднические пояснения сохранены.
- Завершенная карточка дополнительно содержит статичный `.vates-material-complete-banner` сразу после обычной шапки. Плашка подтверждает сохранение и показывает метку «Готово», но не перекрывает тип материала и действия администратора.
- Пустые разделы по-прежнему сохраняются через внутреннюю запись `isPlaceholder`, но она отфильтрована из карточек, счетчиков и кабинета сотрудника. Администратор видит только пустой контейнер раздела с подсказкой, без автоматически созданного урока или теста.
- Браузерная проверка выполнена на существующем разделе `гргнр`: `0` карточек, одна подсказка `.vates-material-section-empty`, без ошибок консоли. Production-сборка проходит, локальный сервер снова доступен на `http://127.0.0.1:3000`.

## Текущее продолжение: единый визуал каталогов

- В `Education.tsx` удалены студийные номера `01/02`. В `app/globals.css` карточки тем и тестов фиксированы по высоте, а завершенная плашка находится в обычном потоке внутри карточки без изменения ее размера.
- `Documents.tsx` получил классы нового экрана загрузки и библиотеки; загрузка, секции и строки документов стилизованы для обеих тем.
- `Assortment.tsx` получил оболочку дерева, счетчики главных разделов и всех категорий, состояния раскрытия/цели и векторный раскрывающий знак.
- `Products.tsx` получил новую панель действий и поиска, фиксированные карточки и компактную статусную компоновку обязательного товара.
- `AIAssistant.tsx` получил встроенную историю, центральный чат, быстрые вопросы и векторные иконки. На широком экране сетка равна `238px / minmax(0, 1fr) / 278px`, на узком история становится выезжающей панелью.
- В браузере подтверждены: карточки материалов `340 px`, обычные карточки товаров `320 px`, обязательная карточка `154 px`, нулевое горизонтальное переполнение и корректный светлый режим документов.
- Последняя production-сборка проходит, локальный сервер отвечает `200` на `http://127.0.0.1:3000`.
- Существующие изменения и неотслеживаемые файлы в `data/` не удалять и не перезаписывать. В этой задаче записи в JSON не выполнялись. Коммита, push и деплоя нет.

## Текущее продолжение: действия всех разделов

- В `Education.tsx` снято исключение для «Основного раздела»: переименование и удаление теперь доступны во всех группах тем и тестов.
- В `Documents.tsx` снято такое же исключение для основного раздела документов; пользовательские разделы сохраняют прежние действия.
- Обработчики не менялись: переименование переносит все элементы раздела под новое имя, удаление выполняется только после отдельного подтверждения.
- В локальном браузере проверены оба основных раздела материалов: действия отображаются, окна открываются и отменяются без сохранения. `npm run build` и `git diff --check` проходят.
- JSON в `data/` не изменялись. Коммита, push и деплоя нет.

## Текущее продолжение: завершенные карточки материалов

- У завершенной темы и завершенного теста обычная шапка теперь всегда рендерится первой: тип, номер и три административные кнопки остаются доступными.
- Зеленая плашка результата стала компактной статичной строкой под шапкой, поэтому больше не перекрывает редактирование и не требует увеличенного верхнего отступа.
- Нижняя часть использует короткий статус «Изучено» для темы и «Сдан» для теста без повторной крупной карточки результата.
- В локальном браузере подтверждены три действия у завершенной темы, одинаковая высота карточек `340px`, отсутствие внутреннего переполнения и корректный вид в светлой и темной теме.
- `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=edu` подтверждены. JSON в `data/` не удалялись и не перезаписывались; коммита, push и деплоя нет.

## Текущее продолжение: правила предпросмотра теста

- В `Education.tsx` у трех правил перед началом теста удалены номера `01/02/03`, а пояснения сокращены до одной понятной мысли.
- В `app/globals.css` карточки правил используют одинаковую компактную высоту около `86px`, центрирование содержимого и уменьшенные отступы без пустой верхней зоны.
- В локальном браузере подтверждены отсутствие номеров, три равные карточки и отсутствие переполнения в светлой и темной теме.
- `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=edu` проходят. JSON в `data/` не удалялись и не перезаписывались; коммита, push и деплоя нет.

## Текущее продолжение: возврат к материалам

- В `app/components/CustomIcon.tsx` добавлена иконка `arrow-left`; все `.vates-material-back-button` в материалах показывают стрелку влево, а самостоятельные кнопки закрытия справа сохраняют крестик.
- `app/tasks/components/Education.tsx` запоминает прокрутку каталога до открытия темы, теста, редактора или AI-экрана и восстанавливает ее после возврата. Восстановление ожидает полную высоту каталога, поэтому работает и для самых нижних карточек.
- В `app/tasks/page.tsx` очистка `routeId/testId` выполняется с `{ scroll: false }`, чтобы маршрутизатор не сбрасывал позицию после возврата по совместимой ссылке.
- В локальном браузере подтвержден возврат к видимой карточке темы «Открытие смены», теста «Напитки на вынос и допродажа» и после отмены редактора теста. SVG кнопок содержит пути стрелки `M19 12H5` и `M11 6L5 12L11 18`.
- Production-сборка проходит, локальный сервер отвечает `200` на `http://127.0.0.1:3000/tasks?tab=edu`.
- JSON в `data/` не удалялись и не перезаписывались. Коммита, push и деплоя нет.

## Текущее продолжение: пустой блок связанных материалов

- `LinkedMaterialsList` корректно возвращал `null` при отсутствии связей, но `.vates-material-linked-section` оставался в DOM и отображался как пустая полоса.
- В `app/globals.css` добавлено правило `.vates-material-linked-section:empty { display: none; }`, поэтому пустые контейнеры скрываются и в тестах, и в темах.
- В браузере подтверждены высота пустого блока `0`, отсутствие видимой полосы и расстояние `24px` между правилами теста и нижней панелью.
- `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=edu` проходят. JSON в `data/` не удалялись и не перезаписывались; коммита, push и деплоя нет.

## Текущее продолжение: шапка библиотеки документов

- В `app/tasks/components/Documents.tsx` удален предупреждающий баннер перед загрузкой документов; пустого места после него нет.
- Кнопка «Новый раздел» очищена от конфликтующих старых стилей, получила иконку папки и отдельный класс `.vates-documents-create-section-button`.
- В светлой и темной темах кнопка имеет размер `166×42px`, белый текст и контрастный фон; на мобильной ширине занимает всю строку.
- Подсказка пустой библиотеки использует актуальное название действия без старого префикса `+`.
- `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=docs` проходят. JSON в `data/` не удалялись и не перезаписывались; коммита, push и деплоя нет.

## Текущее продолжение: обновление страницы товаров

- В `app/tasks/components/Products.tsx` обновлена композиция экрана: сводка каталога, единая поисковая панель, более ясная боковая навигация, компактный фокус обязательного ассортимента и структурированные карточки товаров.
- Все прежние функции импорта, экспорта, создания, редактирования, скрытия, обязательности и удаления сохранены; менялись разметка и визуальные классы, а не схема данных.
- В `app/globals.css` добавлен финальный адаптивный слой для светлой и темной тем. Карточки каталога уменьшены до одинаковой высоты `286px`, а мобильная сетка перестраивается в одну колонку.
- Поле поиска снова занимает всю ширину своего контейнера и имеет самостоятельную поверхность с фокусом. В обязательной карточке удален повтор кода и приоритета, а светлая тема использует заметный бирюзовый акцент вместо слабого желтого фона.
- Счетчик обязательных товаров вынесен из названия в отдельный индикатор справа рядом с навигацией. Активная кнопка обязательности на карточке использует тот же бирюзовый акцент в светлой теме и предупреждающий цвет фокус-блока в темной.
- В локальном браузере подтверждены отсутствие горизонтального переполнения на `1440px`, размеры первых девяти карточек `283×286px`, отсутствие внутреннего переполнения, поиск шириной `882px`, новая компоновка счетчика и корректные цвета обеих тем. Ошибок в консоли нет; после проверки светлая тема восстановлена.
- `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=products` проходят. JSON в `data/` не удалялись и не перезаписывались; коммита, push и деплоя нет.

## Текущее продолжение: удобство AI-ассистента

- В `app/tasks/components/AIAssistant.tsx` удалены приветственная карточка, быстрые сценарии, оба статуса базы и служебные подписи композера. Экран состоит из компактной истории и большого чистого поля переписки.
- История показывает короткое пустое состояние без декоративного блока и числового счетчика в заголовке. Поиск появляется только после создания диалога; повторное создание пустой сессии по-прежнему предотвращено.
- Верхняя панель чата показывает только название диалога. Композер получил новую текстовую кнопку «Отправить» и отдельный SVG `send` из `app/components/CustomIcon.tsx`.
- В `app/globals.css` добавлен финальный компактный слой: однотонный фон сообщений, небольшая панель диалога, расширенная область переписки и адаптивная кнопка отправки.
- Шапка раздела дополнительно уплотнена: на экране `1298×670` ее высота уменьшена с `98px` до `86px`, а рабочая область истории и чата увеличена с `450px` до `466px` без выхода за нижнюю границу.
- В браузере подтверждены светлая и темная темы, размеры на `1298×670`, активная кнопка отправки, мобильная ширина `390px`, боковая история и отсутствие горизонтального переполнения. Тестовый текст очищен перезагрузкой без отправки AI-запроса.
- `npm run build`, `git diff --check` и ответ `200` от `/tasks?tab=standards` проходят. JSON в `data/` не удалялись и не перезаписывались; коммита, push и деплоя нет.

## Текущее продолжение: результаты тестов и футер

- В `app/components/Footer.tsx` удалены все вертикальные символы-разделители. Ссылки на политику и cookie разделяются только свободным пространством, а копирайт использует точку.
- `app/admin/components/TestResultsModal.tsx` переработан в адаптивный отчет: фильтр сотрудника, три показателя сводки, карточки результатов со статусной палитрой и SVG-кнопка закрытия.
- Финальные классы находятся в `app/globals.css` и используют общие переменные обеих тем. На узком экране подписи сводки переносятся без обрезания, карточки перестраиваются без горизонтальной прокрутки.
- В браузере подтверждены светлая и темная темы, отсутствие `|` во всем футере и отсутствие ошибок консоли.
- `npm run build`, `git diff --check` и ответ `200` от `/admin` проходят. JSON в `data/` не удалялись и не перезаписывались; коммита, push и деплоя нет.

## Текущее продолжение: компактные каталоги и страница товара

- Карточки учебных путей используют устойчивую сетку действий: кнопки «Открыть», «Редактировать» и «Назначить» имеют одинаковую высоту и полные подписи.
- Темы и тесты имеют единую фиксированную высоту `272px`, товарные карточки `258px`, обязательные товарные карточки `210px`. Строки документов уплотнены до минимальной высоты `92px`.
- `Assortment.tsx` сортирует отображаемую копию дерева по числовым префиксам на каждом уровне. `Products.tsx` применяет тот же порядок к боковой навигации. JSON при сортировке не сохраняется и не изменяется.
- Обязательный ассортимент использует три видимые позиции на широком экране, две на среднем и одну на мобильном.
- Нажатие на товар открывает внутреннюю страницу `.vates-product-workspace` без overlay. На ней доступны описание, группа, параметры и административные действия; верхняя и боковая навигация сохраняются.
- Возврат «К товарам» восстанавливает вертикальную позицию каталога. В браузере подтверждены светлая и темная темы, порядок корней `1–8`, отсутствие горизонтального переполнения и свежих ошибок консоли.
- `npx tsc --noEmit`, `npm run build`, `git diff --check` и локальный ответ `200` проходят. JSON в `data/` не удалялись и не перезаписывались; коммита, push и деплоя нет.

## Развернутый релиз редизайна Ватэс

- Код редизайна зафиксирован коммитом `cc4b36f Refresh Vates learning and admin workspace`, отправлен в `origin/main` и развернут на `tea-hub.ru` 19 июля 2026 года.
- До pull сохранен полный серверный `data/`: `/var/backups/tea-hub/20260719-022540/data.tar.gz`. Рядом лежат `data.sha256` и `data-files.tsv`.
- Контрольные суммы 144 файлов совпали после `git pull --ff-only`, после production-сборки и после `pm2 restart tea-hub`. Все 122 JSON-файла валидны.
- Серверная сборка Next.js 16.2.4 прошла, PM2-процесс `tea-hub` находится в статусе `online`, публичные `/` и `/login` отвечают `200`.
- Авторизованная браузерная проверка подтвердила: 7 учетных записей, прогресс сотрудников, 37 материалов, 6 документов и 1 827 товаров читаются из прежних серверных JSON новым интерфейсом. Ошибок консоли нет.
- Миграция JSON не выполнялась и не нужна для этого релиза. На хосте остаются измененные пользовательские JSON и существующий локально измененный `package-lock.json`; не очищать и не сбрасывать рабочее дерево разрушительными командами.

## Опубликовано: мобильный футер

- В `app/components/Footer.tsx` email поддержки стал отдельной ссылкой `mailto:` с собственным адаптивным классом.
- В `app/globals.css` ссылки документов получили равные строки, служба поддержки складывается в две строки до `480px`, а копирайт больше не выходит за экран.
- Локально проверены ширины `320`, `360` и `390px`, TypeScript, production-сборка, `git diff --check` и консоль браузера.
- Изменение опубликовано в составе коммита `f64390b`. Существующий пользовательский `data/tea_hub_dynamic_route_v2.json` не трогать и не включать в будущий коммит без отдельной команды.

## Новые постоянные правила

- Не упоминать модель ИИ и уровень рассуждений без прямого вопроса пользователя.
- Сообщения новых GitHub-коммитов писать кратко и на русском языке по сути выполненного изменения.

## Опубликовано: карточка входа

- В `app/login/page.tsx` удален повторный логотип внутри формы и добавлена компактная текстовая шапка карточки.
- В `app/globals.css` уменьшены отступы только в режиме входа; регистрация, CAPTCHA и обработчики авторизации не менялись.
- Локально проверены вход, переключение в регистрацию, ширина `390px`, отсутствие горизонтального переполнения и ошибок консоли.
- `npx tsc --noEmit`, `npm run build` и `git diff --check` проходят.
- Изменение опубликовано в составе коммита `f64390b`. `data/tea_hub_dynamic_route_v2.json` остается пользовательским изменением и не затрагивался.

## Опубликовано: визуал документов как у материалов

- В `app/tasks/components/Documents.tsx` экран документов получил студию загрузки, поиск, фильтр разделов и карточную библиотеку в визуальной системе «Материалов».
- В `app/globals.css` добавлены адаптивные стили: три колонки на широком экране, две на среднем и одна на мобильном.
- Серверные обработчики и структура данных не менялись. `data/tea_hub_dynamic_route_v2.json` остается отдельным пользовательским изменением и не затрагивался.
- Локально проверены `1280×720` и `390×844`, отсутствие горизонтального переполнения, TypeScript, production-сборка и `git diff --check`.
- Изменение опубликовано в составе коммита `f64390b`.

## Опубликовано: выход из документа на телефоне

- `app/tasks/components/Documents.tsx` открывает DOCX во внутреннем предпросмотре, блокирует фон и поддерживает закрытие кнопкой или `Escape`.
- Резервный просмотр в отдельной вкладке получил заметную верхнюю панель со стрелкой возврата; при запрете `window.close()` выполняется переход в `/tasks?tab=docs`.
- `app/globals.css` фиксирует верхнюю панель и нижние действия внутри `100dvh`, оставляя прокрутку только содержимому документа. DOCX-страницы, изображения и таблицы ограничены шириной телефона.
- Локально проверена ширина `390×844`: кнопка закрытия видна, overlay и modal равны окну, горизонтального переполнения нет.
- `npx tsc --noEmit`, `npm run build` и `git diff --check` проходят. Production-версия проекта запущена локально на `http://127.0.0.1:3000` и должна остаться запущенной.
- Исправление предпросмотра опубликовано в составе коммита `62cd426` и развернуто на `tea-hub.ru`.
- Не трогать и не включать в коммит пользовательский `data/tea_hub_dynamic_route_v2.json`.

## Опубликовано: значки ручного создания

- В `app/globals.css` устранен конфликт `.vates-editor-card-heading span`, который переводил квадрат значка в `display: block` и прижимал SVG к левому верхнему углу.
- Подписи заголовков теперь выбираются только через `.vates-editor-card-heading > div > span`; контейнер значка остается flex-элементом.
- Внутренняя обертка и SVG закреплены в размере `21×21px` и центрируются внутри квадрата `40×40px`.
- Локально проверены ручные редакторы темы и теста: центры контейнера и SVG совпадают, production-сборка проходит.
- Локальный сервер перезапущен и оставлен на `http://127.0.0.1:3000`. Исправление опубликовано в составе коммита `62cd426`.
- Пользовательский `data/tea_hub_dynamic_route_v2.json` не затрагивался.

## Опубликовано: прогресс и карточки сотрудника

- В `app/globals.css` белый контрастный текст закреплен для `.vates-staff-progress-hero` и `.vates-learning-hero`, чтобы общие правила светлой темы больше его не перекрывали.
- Карточки тем, тестов, документов и срочных дедлайнов используют сетку `3 → 2 → 1`; единственная карточка занимает одну колонку, а не всю ширину секции.
- Проверочная страница после измерений удалена. В собранном CSS заголовки имеют `rgb(255, 255, 255)`, пояснения `rgba(255, 255, 255, 0.92)`.
- Локальный сервер должен остаться запущенным на `http://127.0.0.1:3000`. Исправление опубликовано в составе коммита `62cd426`.
- Пользовательский `data/tea_hub_dynamic_route_v2.json` не затрагивался и не должен входить в будущий коммит без отдельной команды.

## Опубликовано: вход и футер в одном экране

- `app/login/page.tsx` добавляет класс `vates-login-viewport` на `html` и `body` только для обычного входа и снимает его при регистрации или уходе со страницы.
- `app/globals.css` объединяет страницу входа и общий футер в сетку высотой `100dvh`; прежнее скрытие футера удалено.
- На мобильном экране сюжетная часть и форма занимают отдельные строки без наложения, а футер использует компактную сетку со всеми ссылками и служебными строками.
- В браузере проверены `390×844`, `726×790` и `1440×900`: `scrollHeight` равен `clientHeight`, футер находится внутри окна. Регистрация имеет `scrollHeight` больше высоты окна и прокручивается.
- `npx tsc --noEmit`, `npm run build` и `git diff --check` проходят. Локальный production-сервер оставлен на `http://127.0.0.1:3000`.
- Изменение опубликовано в составе коммита `62cd426` и проверено на публичном `/login` в размерах `1440x900` и `390x844`. Пользовательский `data/tea_hub_dynamic_route_v2.json` не затрагивался.

## Последняя подтвержденная выкладка

- GitHub: `origin/main`, кодовый коммит `62cd426`.
- Сервер: `/var/www/html`, PM2-процесс `tea-hub` в статусе `online`.
- Резервная копия данных: `/var/backups/tea-hub/20260722-043057/data.tar.gz`.
- Контрольные суммы 142 файлов совпали до и после обновления; 119 JSON-файлов валидны.
- Публичный `/login` отвечает `200`, футер видим, прокрутки и горизонтального переполнения нет.

## Локально после последней выкладки: регистрация

- `app/login/page.tsx` теперь переключает не только `vates-login-viewport`, но и отдельный `vates-registration-viewport` на `html` и `body`.
- `app/globals.css` явно возвращает регистрации вертикальную прокрутку, мобильный жест `pan-y`, обычную высоту документа и видимый общий футер.
- На локальном `390×844` проверены все переходы: вход → регистрация → футер → вход. Регистрация прокручивается, текст и нижние действия доступны, горизонтального переполнения и ошибок консоли нет.
- `npx tsc --noEmit`, `npm run build` и `git diff --check` проходят. Локальный production-сервер работает на `http://127.0.0.1:3000`.
- Изменение еще не опубликовано. Не включать в будущий коммит `data/tea_hub_dynamic_route_v2.json`: это отдельное пользовательское изменение.
